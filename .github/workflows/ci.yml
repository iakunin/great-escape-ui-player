name: ci

on:
  pull_request:
    branches:
      - master
  workflow_dispatch:
#  push:
#    branches:
#      - master

env:
  PROJECT_ID: great-escape-294716
  IMAGE: ui-player

# Test pr
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        env: 
          EVENT: ${{ toJson(github.event) }}
        run: |-
          echo "$EVENT"
